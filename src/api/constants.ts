export const LIVE_MODE = false;

export enum API_CONFIG {

    // common
    APP_STATE = '/app/review', // check api version

    // authentication
    LOGIN = '/api/login', // đăng nhập
}

export enum LINK {
    POLICY = '',
}
